---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: config4edge-service
  labels:
    app: config4edge-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: config4edge-service
  template:
    metadata:
      labels:
        app: config4edge-service
    spec:
      # nodeSelector:
        # kubernetes.io/hostname: aaltosea-node_name-worker
      containers:
        - name: config4edge-service
          image: rdsea/fed_orchestrator_service:1.0
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
          env:
            - name: EDGE_SERVICE
              value: "edge_service"
            - name: EDGE_SERVICE_PORT
              value: "8006"
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: POD_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name